require "spec_helper"

RSpec.describe Ragent::Plugin::Fswatcher do
  it "has a version number" do
    expect(Ragent::Plugin::Fswatcher::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
